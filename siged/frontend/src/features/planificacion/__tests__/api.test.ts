import { afterEach, describe, expect, it, vi } from "vitest";
import { planningApi } from "../api";
afterEach(()=>vi.restoreAllMocks());
describe("planningApi route contracts",()=>{it("uses DRF action URLs and parent fields for every aggregate",async()=>{const fetchMock=vi.spyOn(globalThis,"fetch").mockResolvedValue(new Response(JSON.stringify({count:0,next:null,previous:null,results:[]}),{status:200}));await planningApi.plans("t",2,"?page=1");expect(fetchMock.mock.calls[0]?.[0]).toBe("/api/planes-estudio/instituciones/2/?page=1");fetchMock.mockImplementation(async()=>new Response(JSON.stringify({id:1}),{status:201}));await planningApi.createGrade("t",7,{nombre:"First"});expect(fetchMock.mock.calls[1]?.[0]).toBe("/api/grados-escolares/");expect(JSON.parse(String((fetchMock.mock.calls[1]?.[1] as RequestInit).body))).toMatchObject({plan_estudio:7});await planningApi.createSubject("t",9,{nombre:"Math"});expect(fetchMock.mock.calls[2]?.[0]).toBe("/api/asignaturas/");expect(JSON.parse(String((fetchMock.mock.calls[2]?.[1] as RequestInit).body))).toMatchObject({grado_escolar:9});});});
