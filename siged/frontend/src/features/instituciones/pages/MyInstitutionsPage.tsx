import { useEffect,useState } from "react";import { useAuth } from "@/features/auth/hooks/useAuth";import { institutionApi } from "../services/api";import type { Institution } from "../types/institucionTypes";
export function MyInstitutionsPage(){const {token}=useAuth();const [items,setItems]=useState<Institution[]>([]);useEffect(()=>{if(token)institutionApi.mine(token).then(setItems)},[token]);return <div className="space-y-8 p-8"><header className="border border-t-4 border-t-primary bg-heading-block p-6"><h1 className="text-3xl font-bold">Mis instituciones</h1><p>Acceda y gestione sus instituciones educativas asignadas.</p></header>{items.length===0?<p>No tiene instituciones asignadas activas.</p>:<div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{items.map(i=><article className="flex min-h-72 flex-col border bg-surface text-center shadow-sm" key={i.id}><div className="flex flex-1 flex-col items-center justify-center p-6"><span className="material-symbols-outlined text-7xl">school</span><h2 className="text-lg font-bold">{i.nombre}</h2><p>Código de institución: {i.codigo}</p></div><button className="bg-primary py-4 font-bold text-white">Ingresar <span className="material-symbols-outlined">check</span></button></article>)}</div>}</div>}
